<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyKotlin">

        <!-- MainActivity : Activity الرئيسية للتنقل -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="My Kotlin"
            android:theme="@style/Theme.MyKotlin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SharedPreferences Activity -->
        <activity
            android:name=".SharedPreferencesActivity"
            android:exported="false"
            android:label="SharedPreferences Example"
            android:theme="@style/Theme.MyKotlin" />

        <!-- SQLite Activity -->
        <activity
            android:name=".SQLiteActivity"
            android:exported="false"
            android:label="SQLite Example"
            android:theme="@style/Theme.MyKotlin" />

    </application>

</manifest>